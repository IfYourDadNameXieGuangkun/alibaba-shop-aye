spring:
  cloud:
    stream:
      rocketmq:
        binder:
          namesrv-addr: localhost:9876
      bindings:
        EMAIL_OUTPUT: {destination: topic-email, content-type: application/json, group: group-email}